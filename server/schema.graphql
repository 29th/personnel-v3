type User {
  id: Int!
  firstName: String!
  lastName: String!
}

input UserCreateInput {
  firstName: String
  lastName: String!
}

type Query {
  users: [User!]!
}

type Mutation {
  createUser(data: UserCreateInput!): User
}
